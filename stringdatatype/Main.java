package stringdatatype;

public class Main {
    /**
     * String is a class containing static methods. Return type string
     * String objects - variables of time string, objects have methods(not static)
     * String str = "abc";
        is equivalent to:

        char data[] = {'a', 'b', 'c'};
        String str = new String(data)
     * 
     */
    public static void main(String[] args) {
        //Calling methods in java
        String text = "This is an object";
        text.toUpperCase();
        
    }
}
